_target_: dyno.dataloading.dataloaders.AudioDataModule

train_csv: null
val_csv: null
test_csv: null

# Metadata for pre-extracted embeddings. These fields are intentionally kept in
# the experiment config/signature so trained checkpoints identify their source.
embedding_encoder: unknown
embedding_rate: unknown
embedding_dim: ${model.embedding_dim}

path_col: npy_path
n_frames: 60
random_crop: true
batch_size: 32
num_workers: 4
limit_n: null

target_sr: 48000
target_n_samples: 96000
